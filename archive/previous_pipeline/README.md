# Previous pipeline archive

This directory is not part of the active project.

It contains the superseded `src/` workflow, the earlier `gene_tree_generation/`
workflow, historical tests, the long-form Stage 1 record, and old generated
outputs. They were moved here during the project-wide reorganization so the
active root contains one implementation while the earlier work remains
recoverable.

Use `run_project.py`, `cse6406/`, and the root documentation for all current
work. Nothing under this archive is imported by the active pipeline.
